# modules/lamr — LAMR Structured Evidence (FR-08)

No code yet.

## Planned
- `data/lamr.ts` — LAMR reports fetch/map (`lamr_reports` gains a `module_id` FK → planned `program_modules`; one LAMR per module per batch)
- `ui/` — LAMR evidence capture/review components

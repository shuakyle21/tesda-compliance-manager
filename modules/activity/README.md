# modules/activity — Activity Log and Audit Trail (FR-12)

No module code yet. The activity-log page (`app/(dashboard)/activity-log/page.tsx`) currently reads `MOCK_ACTIVITY` from `shared/mocks`.

## Planned
- `data/activity.ts` — audit-trail fetch/map
- `ui/` — activity feed components extracted from the page when live data lands

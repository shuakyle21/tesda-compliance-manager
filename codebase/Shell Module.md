# Shell Module

**FR:** none — app chrome
**Path:** `modules/shell/` (`ui/`)
**Status:** partial — ui layer (Sidebar → Topbar → MetricsRow)

The app shell that wraps every route. No functional requirement of its own; composes the navigation and top-level layout that module screens render into.

**Concept:** [[Architecture Overview]]

[[Codebase Map]]

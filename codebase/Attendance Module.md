# Attendance Module

**FR:** FR-07 — attendance and training progress
**Path:** `modules/attendance/`
**Status:** stub — README only, no code yet

Progress = `sessions_held / total_sessions` (nominal hours ÷ 8, snapshotted on the batch). A scholar with ≥5 absences is ineligible (locked ADR-001 rule).

**Concept:** [[Attendance And Progress]]

[[Codebase Map]]
